pipeline {
    agent any

    stages {
        stage ('git clone') {
            steps {
                echo 'stage git clone'
            }
        }
        stage ('build') {
            steps {
                echo 'stage build'
            }
        }
        stage ('deploy') {
            steps {
                echo 'stage deploy'
            }
        }
    }
        
}